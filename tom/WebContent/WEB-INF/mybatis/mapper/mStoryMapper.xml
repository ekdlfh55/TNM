<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="story">
		<insert id="insertmStory" parameterType="com.sp.story.story">
		INSERT INTO mstory(userId, subject, content, hitCount, created, saveFilename, orginalFilename)
			VALUES(#{userId},#{subject},#{content},#{saveFilename,jdbcType=VARCHAR},#{originalFilename,jdbcType)
		</insert>

</mapper>